---
const items = [
  'Monday LA Salsa',
  'Thursday Bachata',
  'Cali Salsa workshops',
  'Beginners welcome',
  'No partner needed',
  'Merch out now',
];
---
<div class="hero-marquee">
  <div class="hero-marquee-track">
    <span>
      {items.map((item) => (
        <>{item} <span class="dot"></span> </>
      ))}
    </span>
    <span>
      {items.map((item) => (
        <>{item} <span class="dot"></span> </>
      ))}
    </span>
  </div>
</div>
